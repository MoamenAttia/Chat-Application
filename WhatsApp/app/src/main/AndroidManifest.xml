<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moamen.whatsapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.ActivePeople" />
        <activity android:name=".View.ChatRoom" />
        <activity android:name=".View.CreateRoom" />
        <activity android:name=".View.Home" />
        <activity android:name=".View.EnterChatRoom" />
        <activity android:name=".View.CurrentChatRooms" />
        <activity android:name=".View.SignUp" />
        <activity android:name=".View.SignIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>